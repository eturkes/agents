# Fetched fresh from upstream on every `container/aeon/upgrade` (aeon) or
# `host/cachyos/upgrade` (cachyos) run, so it is not tracked here (see the
# "Docs mirror" section in either script).
/docs/agentlanguages.dev/llms.txt

# Machine-local Claude Code state: personal settings overlay + Headroom wrap runtime marker.
/.claude/settings.local.json
/.claude/.headroom_wrap_marker.json
