# Environment

- CachyOS (Arch) workstation; you + all sessions/subagents = sole user, running as `eturkes` with passwordless sudo, full r/w, network.
- Desktop/computer use: a full X11 session is live, with a wide range of already-authenticated GUI apps available.
- Resolve user-supplied paths before the first absolute-path call: expand `~` from the active `$HOME` (`/home/eturkes`) and use `readlink -f` when the path exists; derive any home path from that resolved output.
- Workflow defaults: Python → `uv`; Node.js → `pnpm`; visual QA/web scraping → `chromiumfish`.
- Compute: prefer the discrete GPU where applicable; display/video use the iGPU, leaving discrete VRAM dedicated to compute.
- Freely modify env + yourself (skills/plugins) + install anything; persist through blockers; when truly stuck, ask.
- Authenticated web: for research/retrieval, assume BrowserOS MCP (`http://127.0.0.1:9000/mcp`) can access anything available in my signed-in day-to-day browser, including university access to most peer-reviewed journals. `chromiumfish` = isolated visual QA. Paywall/auth/human gate surviving every route → ask me, then continue.
- Post-work: thoroughly clean task-touched paths, especially `$HOME`; remove temporary/stale artifacts + dangling symlinks.
- Headless capture: use `$(chromiumfish path) --headless`. Screenshots: `--screenshot=<path>`; full-page: `--print-to-pdf=<path> --no-pdf-header-footer`. URL fragments render blank here. `--force-dark-mode` leaves `prefers-color-scheme` unchanged. SwANGLE/Vulkan `EGL` initialization errors plus `Exiting GPU process` are benign when the command succeeds and produces real output.
- `grep` = CC shell-fn shadow → `rg-fff` (**RE2**, relevance-ranked, fuzzy-fallback; see `# fff`), distinct from raw `ugrep -G`; `find` = CC shell-fn shadow execing `rg-fff` under `ARGV0=bfs` (GNU-compatible incl. `-printf`/`-regex`/`-exec`; errors prefix `bfs:`; GNU binary → `command find`) → always TYPE `find`; `bfs` is absent from PATH, so a literal `bfs …` exits 127 printing nothing, which reads exactly like a zero-match result. `rg`=`/usr/bin/rg` (unshadowed). System `grep` binary = GNU grep (BRE). Byte-exact/clean → `command grep` | `/usr/bin/rg`.
- Byte-equality → prove with `cmp`/`sha256sum`; real diffs → `git diff --no-index`.
- Shell exit codes → capture immediately (`cmd; rc=$?`) before any `printf`, substitution, or next command + label it; every command overwrites `$?`. Where EMPTY output IS the finding (no matches, nothing running, nothing modified, nothing recently written), report rc beside it + pair the query with a positive control that must print: a missing command (127), a mistyped path + a glob matching nothing all emit byte-for-byte what a true negative emits.
- `pgrep -f`/`pkill -f` can self-match their `bash -c 'eval …'` wrapper → use one bracketed pattern (`index[.]js`) + `|| echo none` per command; separate kill/relaunch calls. CC's `pkill` guard blocks CLI-matching patterns.
- `bgcmd` (`~/.local/bin/`) = filesystem REPL, objects persist across separate Bash calls: `export BGCMDDIR=<dir> BGCMDPROMPT='>>> '` (re-export each call) → `bgcmd START <interp> -i -q` → `bgcmd '<oneliner>'` → `bgcmd 'exit()'; rm -rf "$BGCMDDIR"`.
- Bash heredoc writes gate on the `cat` TOKEN, not the redirection (deny matches the bare name in any position, scratchpad no-prompt design included): `cat > f <<'EOF'` + `cat >> f <<'EOF'` = DENIED; `tee f <<'EOF'`, `tee -a f <<'EOF'`, `printf … >> f`, fileless construction (inline data as program args) + the Write tool all pass → append to a live scratch roster/log with `tee -a`.
- Deferred tools: session-start list = ground truth (listed → exists). Direct best-guess calls RUN despite the boilerplate warning → call first. `tool_search_tool_regex` (`pattern` = Python `re.search`) scopes to ALREADY-LOADED schemas → its `No tools match` on a deferred name = load it, then search. `InputValidationError` = fetch that schema. Always emit an MCP tool's FULL registered `mcp__<server>__<tool>` verbatim from that list; a name matching no unique alias (server segment dropped → `mcp__<harness-prefix>__<tool>`) aborts THAT response stream server-side — `400`, or `cannot restore Claude OAuth MCP tool alias "<name>": no unique request-local match`. Stream-only: the bad `tool_use` stays out of the transcript + neighbouring turns keep succeeding → session remains live; error turns are model-invisible → retry/`Continue` re-emit verbatim → recover in-session by naming the exact alias in the next turn. `400 … unexpected tool_use_id` = history corruption → `/rewind`. System-prompt-layer fixes load next fresh session/compaction.
- Finite background wait → `run_in_background: true` + `TaskOutput(task_id, block=true, timeout=…)` (deprecated-but-live; completion also delivers a `<task-notification>` with the output-file path → `Read` it); foreground `until` loops auto-background, so express shell polling as hook-accepted `until` loops. `timeout` is milliseconds, capped at 600000; repeat 10-minute blocks. A seconds-valued timeout returns immediately while the task continues. Piping a background command through `tail`/`head` withholds ALL output until exit → interim polling reads an empty file; redirect to a log (`> file 2>&1`) and read that, or let the command print unpiped.
- Serena user-scope MCP = live-LSP primary (nav/edit/rename/diagnostics; ~70 langs, auto-install); project memory remains canonical. Missing language → `global` LSP marketplace gap plugin (registered here; activate per project). At first touch, add real code + edited formats (markdown/toml/yaml/json) to `.serena/project.yml` `language_servers:`, then restart once (startup-only); keep formats with useful diagnostics. Headroom `wrap` registers Serena as its code-memory MCP + deletes only its own (ledger-recorded) entry; user-registered entries persist.
- Serena ops: missing target in `Active language servers` → add to `language_servers:` (first = fallback), restart, verify by symbol call. `replace_symbol_body` spans leading doc + outer `#[...]` attrs → include them or use `replace_content`. Missing/new `project.yml` keys trigger a full annotated rewrite; values persist → verify `language_servers:`/`ignored_paths:` + track the template. Serena owns `.serena/.gitignore` → commit `.serena/`; root `.gitignore` = project rules. Diagnostics: Serena-served formats require explicit `get_diagnostics_for_file`; `global` plugin formats push passively. MCP registration passes `--project-from-cwd` → non-project cwd (e.g. `$HOME`) = `known projects: []` on every call → always `activate_project <abs path>` before symbol work. `restart_language_server`/`remove_project` = context-excluded → LS restart = reconnect the MCP server; deregister = edit `~/.serena/serena_config.yml` `projects:`.
- Serena MCP = per-session stdio spawn (`uvx` git-resolve → GitHub each start) → a transient resolve failure starts the session with serena disconnected (no new `~/.serena/logs/<date>/mcp_*` = died pre-log; CC never retries mid-session). Registration stays valid → confirm `claude mcp list` (spawns fresh, reports Connected); recover via `/mcp` reconnect (user) or next session; interim: `Read`/`/usr/bin/rg`, or drive serena out-of-band (`uvx --from git+https://github.com/oraios/serena serena <cmd>`).
- Serena file/bulk edits: extensionless script + empty symbol overview → use `Read`/`rg`. `replace_in_files.paths_include_glob` remains project-root-relative when `relative_path` narrows selection → prefix the directory or select unrestricted paths. Dry-run `0 file(s)` = selection mismatch; `0 occurrence(s)` = text mismatch.
- Marksman = Serena's bundled Markdown link LSP; fixed config, pull diagnostics source `Marksman`; all findings = code 2/Warning → keep the full signal. Fixes: bare `][` outside code parses a phantom reflink → backtick notation; missing document → repair link; >1 H1 target → retain one H1. Bare `<nonterminal>` → backtick-wrap (parser treats it as HTML; link lint skips it). Byte-authoritative edge-space code spans → visible ␣=U+0020 legend or fence (CommonMark trims one pair). Index honors `.ignore`/`.gitignore`/`.hgignore` at startup; keep Markdown targets index-visible and use `.rgignore` for rg-only hiding. Disable: remove `markdown` from `language_servers:` + restart.
- Markdown gate: prefer `markdown` active in Serena + pull `get_diagnostics_for_file`. Before activation, check bare `][` outside code, Markdown links resolving to existing `.md` (write other paths inline), and one H1/file. Authoritative stopgap (binary lands with Serena's first `markdown` LS start): find it (`find ~/.serena -name marksman -type f`); run `marksman server`; send `initialize` → `initialized` → `textDocument/didOpen`; read `publishDiagnostics`. A `.git`/`.marksman.toml` root enables cross-file links. Seed one broken link as a positive control because a clean file is silent. Marksman emits all findings together + indexes every committed `.md`, including `.agent/`.
- Headroom = pacman/AUR package; the image-worker hard-termination fix (upstream-pending) deploys as a patched uv-tool build shadowing it on PATH — state+patch+recipe+revert → `~/Projects/agents/host/cachyos/headroom/README.md`.
- Docs mirror `~/Projects/agents/docs/<site>/llms.txt` (scopedcommits.com, agentlanguages.dev) > web fetch.

## Reading

- Reader gates: `permissions.deny` catches dump commands (`cat`/pagers/hex/editors) in every `Bash` segment incl. pipes + compounds; `head`/`tail` shell-fn guards (`claude/read-guard.sh` ← `~/.profile`, captured into CC's snapshot) catch the file-reading form alone → `cmd | head -N`/`| tail -N` pass through, `command head` bypasses. Headroom excludes `Read` from lossy compression + reclaims its bytes once stale/superseded (file_path-keyed); `Bash` output = designated compression target, never reclaimed.
- Text inside a binary (JS bundled into an ELF, e.g. the `claude` CLI at `~/.local/share/claude/versions/<v>`) → `/usr/bin/rg -a -o '<pat>.{0,400}'`; `-a` is required, since plain `rg` prints `binary file matches` and withholds every line. `Read` returns mojibake and `strings`/`xxd`/`od` are deny-listed, so `rg -a` is the readable path. Widen with `.{N}` on both sides to walk minified call sites.
- Headroom reads = lossy/reversible. `[N … hash=…]` → `mcp__headroom__headroom_retrieve`; hashless `[N lines omitted: …]` → narrow `Read`/`grep -n`. Treat compressed output as browse context. Byte-exact `Edit.old_string` → retrieve/narrow Read/grep/sed/`read_text()`, anchored on short distinctive bytes; compression rewraps prose and can mislead with `\uXXXX` hints. Expired/evicted hash 404 → narrow reads/Python.
- Retrieve tool's sole name = full `mcp__headroom__headroom_retrieve` (server + tool segments both read `headroom`; naming rule → `# Environment`). Large retrieve may return `<<ccr:…,NN KB>>` → use narrow `Read`/`grep -n`/Python.
- Quote YAML frontmatter scalars opening with an indicator char (`[ { } ] , & * ! | > % @ # :`, backtick, double-quote): leading `[` → flow sequence → `ParserError` or silently-dropped field. Verify ad-hoc frontmatter with an ephemeral `pyyaml` parse.
- `API Error: <ConnectionTerminated error_code:0 …>` = transient HTTP/2 GOAWAY mid-stream through Headroom; context survives → `git status`, resume.
- Context occupancy = latest assistant `usage` sum (input + cache create/read + output) = window cost on ordinary turns; it exceeds transcript size (system/tools/CLAUDE overhead + redacted reasoning stay billed — high readings are real; CLAUDE markers can double at boundaries). Server-tool turns (e.g. ToolSearch) bill every internal iteration — cache_read stacks ~1 window per iteration → read occupancy from an ordinary turn. `context-gauge` = the sum, `jq .message.usage "$(context-gauge -p)"` = its parts; effort shifts the usage/stored ratio → remeasure. Auto-compaction fires in every context (main + subagents) when last usage sum + pending payload crosses 240K (warning at 220K) → a big payload overshoots the threshold, and an iteration-inflated sum fires it while real occupancy sits far lower → hold the 200K aim + 40K reserve and run tool discovery early.

## fff search (`rg-fff` — shadows `grep`)

CC maps `grep`→`rg-fff`: default fff RE2, relevance-ranked + marked. Serving requires repo cwd; other contexts defer to ugrep. `rg`=`/usr/bin/rg`. Per-repo daemon auto-spawns. Fresh writes can lag its index → use `command grep` or re-query.

- **Output ≠ real grep** → RANKED by relevance over file-order, strips leading `./`, inline markers, lines capped 512B. Clean/order-stable/byte-exact/full-line → `command grep` (=GNU grep BRE, `./`, file-order, untruncated) | `/usr/bin/rg`.
- **Zero-match grep ≠ empty (BIG)**: auto fuzzy-fallback → `# rg-fff: 0 EXACT…` hdr + N ` [~approx]` lines on **STDOUT** (survive `2>/dev/null`, flow into pipes/`$()`), shaped like real `path:line:text`. Truth = **exit 1** + `[~approx]`/leading-`#` (lines appear regardless) → piping/capturing a maybe-zero grep MUST filter `[~approx]`/`#` or use `command grep`. Off: `RG_FFF_NO_FUZZY_FALLBACK=1`.
- **`[def]`** trailing tag = likely definition; rank treats it like any other line — junk/long lines can outrank it → trust the tag over position; "read top hit first" misleads here.
- **>512B line** → served truncated + ` [...rg-fff: line truncated at ~512B; Read the file for the full line]` (`-c` counts it once); non-UTF-8 (U+FFFD) long line defers instead → Read file for full line.
- **Served**: literals incl. phrases w/ space/`:`/quote (regex-escaped), RE2 regex (`\b \d \w + * ( ) | {} ?`, empty-capable `x?` OK), `-i`, `-A/-B/-C N`, `-l`, `-c`, `--include=*.ext`, reldirs + `.`. **Serve requirement: add `-r`/`-R` to every fff query.** Verify via `RG_FFF_LOG=<path>` (`served-daemon`/`served-cold`); a missing flag yields `fallback-ineligible` + deferred BRE, where RE2 syntax can silently misfire (`grep -c 'a|b' <dir>` = 0, exit 1).
- **DEFERS → embedded ugrep -G = BRE** (clean, file-order, no markers): missing `-r`/`-R` (ALL queries — the most common cause by far), non-repo cwd (ALL queries), stdin/pipe (`…|grep` ALWAYS), single-file arg, `-o`, `-P`/PCRE, regex matching newline (`\s`/negated class), non-ASCII pat, `--no-ignore`/`--include-dir`, abs path, glob ≠ `*.ext`. **Deferred regex = BRE** → RE2/ERE syntax misfires (piped `foo|bar` → matches literal "foo|bar").
- **Dialect when SERVED = RE2** → bare `|`=alt, `+ * ?`=quant, `()`=group, `\d \w \b` classes; `\|` `\+` = LITERAL. Old BRE `\|`=alt / `\+`=1+ advice now valid ONLY for `command grep` / deferred path.
- `/usr/bin/rg` direct calls: recurses by default → pass `<pat> <path>` ALONE. `-r` = `--replace`, so carrying the shim's mandatory `-r` eats the pattern as replacement text + promotes the path to pattern → readable stdin (agent shell) blocks until the harness timeout; `.` matches every line + rewrites output to the replacement (rc=0, fabricated bytes shaped like real matches); a named dir matches nothing (rc=1). Reach dot-dirs (`.agent/`, `.scratch/`) by NAMING the path — explicit paths search regardless of hidden/ignore state; tree-wide sweeps take `--hidden`; gitignored dot-dirs take `-uu` (= `--hidden --no-ignore`), which `--hidden` alone MISSES.
- Knobs: `RG_FFF_FIRST=0` = regex→byte-equiv BRE-mirror (literals STAY fff-ranked+marked); `RG_FFF_LOG=<path>` per-call decision log (`served-daemon`/`fallback-*`); `RG_FFF_DEBUG=1` serve/defer reason on stderr.

## Subagents

- Keep working while delegated agents run. Supply relevant context; intervene on drift. Chunk sequentially around rate limits. Resolve each agent AT HARVEST — read its report, then `TaskStop` it by name in that same call: a DONE marker ends the report, not the agent, and an unstopped agent keeps its full write allowlist. Close in the order stop-all + retire watchers → prove quiescence → remove any worktrees + `wt/` branches → decisive rerun → commit, since one late write between the verification and the commit leaves every recorded number describing a tree that no longer exists; this sequence is stated here alone, every mode + project loop doc citing it and adding only its own bindings (harvest content, ledgers, gate command).
- Name every lead dispatch (teammate) — net-resilient in every role: a dead teammate revives in-place via `SendMessage` with full context retained and the same channel steers mid-run, while an unnamed death forfeits the dispatch. Agents execute their scope directly — lead approval gates re-delegation; teammate-internal subagents stay unnamed (their completion → inline `<result>`). Cross-session communication relays through user/current lead.
- Teammate protocol (`Agent` returns spawn metadata; completion/death = pull-only — no notification): assign each a self-identifying completion marker `<NAME>-DONE-<i>` (agent name verbatim, fresh each dispatch) + a report destination (scratch file for findings/evidence; working tree for code). Two independent counters: role-name `-<k>` = instance ordinal (monotonic, gapless, never reused), marker `<i>` = iteration within that instance → the name↔instance map survives compaction. Poll the transcript's LAST assistant text for the marker (`jq -r 'select(.message.role=="assistant") | .message.content[]? | select(.type=="text") | .text' <t>.jsonl | tail`); raw-transcript `rg` false-positives on the spawn prompt + every `SendMessage` body carrying the marker, and anchored `"text":"<MARKER>` misses end-of-message markers. Watch loops + polls match marker OR death shape in one grep (`grep -qE 'MARKER|API Error'`) so death ends wait instantly; on death act immediately per death bullet. Watcher lifetime = teammate lifetime: each binds ONE transcript path + ONE marker → kill it in the same call that `TaskStop`s or supersedes its teammate, since a survivor later fires the OLD transcript's tail shaped exactly like the successor's completion. Confirm every completion signal against the awaited teammate's own transcript path. Completion = marker AND transcript silence (~90s), never the marker alone: a resumed agent runs with tools live while its last assistant text still reads DONE from the previous iteration, so a marker-only poll reports finished agents that are mid-run and actively rewriting their reports. `SendMessage` = plain text + required `summary` (teammate protocol frames rejected); messaging a completed agent RESUMES it with tools + write allowlist intact — from the lead OR from a peer teammate, a wake the lead never observes → `TaskStop` at harvest is what keeps a finished agent out of a successor's files; read status from the transcript. `TaskOutput` = background Bash task IDs. Report files: confirm each with `ls` — a teammate can report writing a file it never wrote, and some decline file writes entirely (citing a higher-priority harness rule) while delivering the full report in their final message → re-request the write once, naming the exact absolute path, else persist it yourself by extracting assistant text from the transcript. Teammate evidence artifacts (probe CSVs, run logs, staged copies of the code under test) bind to no source state + outlive the repair they appear to certify → re-run or re-derive before crediting; un-re-derivable artifact = browse context.
- Delegation boundary block = per dispatch, each item its own directive line: git posture (`leave every change unstaged in the working tree; the lead alone runs git add/commit`), write allowlist, gate + spec surfaces marked `read-and-conform-to` (agent REPORTS one it judges wrong → lead rules). Absent those lines the defaults bite: working-tree scope ⇒ the agent stages, commits + edits neighbouring coordination files; acceptance `gates green` ⇒ it edits the gate. Parenthetical asides read as advisory. The lead's brief honours the same split, since delegating a lead-retained file re-frames every retained boundary as negotiable. First breach ⇒ revert + restate; second breach same agent ⇒ `TaskStop` + fresh successor (saturated agents rationalize rather than re-read).
- Shared mutable resources (build/target stores, DBs, ports, a single work tree, one live browser profile) = one holder per wave, named in every brief + enumerated, because `analysis-only` reads as "tracked files stay byte-identical" and leaves a reviewer free to rebuild the shared store. Unassigned token shows up as agents burning turns on `ps`/`pgrep` hunting each other. A teammate reporting its files changing underneath it = CONTENTION until disproven — believe the reporter, then sweep transcript mtimes over every agent spawned this session (`find "$HOME/.claude/projects/$(pwd -P | tr '/.' '--')"/*/subagents -name '*.jsonl' -mmin -2`; `-mmin` works under both the `find` shadow + GNU, relative `-newermt '-90 seconds'` is GNU-only), since a live agent writes continuously. Sampling one artifact for stillness proves only that nobody wrote during that window. An empty sweep counts as quiet once a wide-window rerun (`-mmin -100000`) lists the roster's transcripts — otherwise the glob matched nothing and `quiet` is vacuous. Run the sweep on EVERY poll cycle, not only on a report: one `find` folded into an existing loop keeps the live set already known when an inconvenient claim lands, whereas the same check first reached for mid-close gets weighed against carrying on at the moment it is least welcome.
- Worktree isolation — use where a wave writes code: one `git worktree` per teammate, lead alone writing the primary tree, which confines the contention rules above to it. `git worktree add -b wt/<name> <scratch>/worktrees/<name>` = tracked-content sized + sub-second, since build outputs + environments stay untracked; the brief carries that path as the teammate's whole world. Give each worktree private cache/coverage/state paths + point its toolchain env at the primary environment read-only → every worktree runs the full gate concurrently off one shared environment. Teammates commit in-worktree per checkpoint; baseline restore = `git reset --hard` + `git clean -fd` back to the last checkpoint, new files included. Harvest merges content with `git merge --squash wt/<name>`; a successor inherits its predecessor's worktree, whose fetched pages + derived data outlive the death that stranded them.
- Permission mode = `bypassPermissions` everywhere (settings `defaultMode` + agent dispatch); `permissions.deny` still fires.
- Fixed subagent model + effort: every subagent / teammate runs `gpt-5.6-sol` — settings env pin = authoritative, per-call `model` inert; `model` = blank so effort env carries through.
- Scope each agent to finish + report inside ~200K, with auto-compaction as the safety net (work continues + stays steerable post-compact). Gauge every live teammate every poll cycle, all roles alike (implement, review, research + one-off producers saturate at comparable rates); >~180K ⇒ checkpoint to its report + hand off to a fresh same-role successor. A report emitted >~90% is context-starved — compressed, follow-up-incapable, trading correctness for closure → re-derive before crediting. Budget margin + split large rewrites at section boundaries: a read+rewrite agent can peak ~100K on ~40KB (~37%); ~180 web-tool calls ≈ one full window → cap research agents near ~100 web calls. Transcripts: `~/.claude/projects/<project>/<session>/subagents/agent-a<name>-<hash>.jsonl` — the `a` prefix precedes the agent name, so glob `agent-a<name>-*.jsonl`. Health gauge = `.message.usage` (gpt-5.6-sol reasoning blobs inflate file size ≈ 100× content) → `context-gauge [-p] [<name>]` (`~/.local/bin`, cwd-scoped): bare = `N% NK/240K` (MAIN = last turn, teammate = high-water), `-p` = that transcript's path, unresolved name = `? ?/240K`; mid-run assistant text ≈ nil → harvest reports/diffs.
- Deliverable-first checkpointing, any enumerable output (rows, records, probes, cases, findings): brief names unit total + batch size; the teammate's FIRST write seeds the WHOLE skeleton — every target unit already a row, each cell `unknown` — ahead of any data-gathering call, then fills cells in place every batch in the deliverable's FINAL format ⇒ artifact whole from call one, each later write an improvement. Brief binds a self-enforced cadence (write ≤ every N tool calls) + ships unit 1 by the simplest available means, funding tooling only after that first flush — a "gather everything → derive → emit" pipeline reports 0 units for its entire run, however healthy its gauge. Lead polls the report's DELIVERABLE-UNIT COUNT — bytes/mtime/liveness/gauge all read green while the deliverable table holds 0 rows. Count flat across 2 polls or lagging its gauge ⇒ order an immediate flush, then succeed the agent. Mid-run `SendMessage` lands at a turn boundary alone → a long uninterrupted tool loop absorbs no steering, so the brief carries every correction the run needs + the report file stays the control surface. Unflushed work = raw tool results in the transcript alone → re-run over mining it; on-disk artifacts (fetched pages, derived data) outlive the agent → a successor resumes from them.
- gpt-5.6-sol death shape: agent's final assistant text = `API Error: … help.openai.com … request ID …` | `API Error: 503 auth_unavailable … (providers=codex, model=gpt-5.6-sol) … gateway (127.0.0.1:8787)` = provider-side flapping (`~/.codex/auth.json` JWT stays valid; 8787 gateway = MAIN's own transport — leave running). Respond with cadence: revive (`SendMessage`) on sight; instant re-death = active outage → retry every few minutes while MAIN keeps working; 2 consecutive re-deaths or transcript >~180K → switch to fresh successor from teammate's checkpoint/report (fresh + small outlives revive under flapping). Deliverable-first checkpointing ⇒ death anytime = respawn-cheap.
- WebSearch budget = 200/session (`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`), shared by MAIN + every subagent; two research agents at ~75–80 calls each exhaust it — over-budget WebSearch fails while other web tools keep working → ration an explicit per-agent allowance on research fan-outs.
- Main-session auto-compaction: running background agents + bg shells continue straight through the boundary; ids + `TaskOutput`/task-notifications stay valid after it. Harness re-announces running agents post-boundary (id + partial-output path); bg-shell ids persist only via your own notes → multi-agent fan-outs: keep a scratch roster (name | transcript | expected marker | status | report) from first spawn; near the limit, externalize bg-shell ids + expected results there too.

## Meta

- Symlinks (any path): `Read` follows them, `Edit`/`Write` refuse a symlinked FILE ("Refusing to write through symlink") → edit the `readlink -f` target, reach it via a symlinked PARENT DIR (allowed), or write through `Bash`.
- `Edit` line deletion → trailing-newline form (`victim\n`→``) or anchor-keeping replace (`anchor\nvictim`→`anchor`); leading-`\n` old_string + empty new_string also eats the trailing newline → neighbors merge on a success-reported edit (multi-line inserts clean). Post-edit `command grep -n` a neighbor line.
- My direct instructions outrank any `CLAUDE.md`.
