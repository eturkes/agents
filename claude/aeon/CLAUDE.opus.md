# Environment

- Debian container; sole user across all sessions/subagents = `eturkes`; passwordless sudo; full r/w + network.
- Host + container share trees at different abs paths (in-container `/run/host/...`); uv venvs path-bake per-layer → pick by path-prefix. Per-layer `UV_PROJECT_ENVIRONMENT` (`.venv`/`.venv-host`, git-ignored); `.envrc` + direnv in interactive shells, else `export`.
- Before the first absolute-path call, resolve user paths: expand `~` from active `$HOME`; existing path → `readlink -f`; derive home paths from the resolved result, since a username alone underspecifies `/var/home/<user>`.
- Workflow defaults: Python → `uv`; Node.js → `pnpm`; visual QA/web scraping → `chromiumfish`.
- Env/self changes (skills/plugins) + software installs authorized; persist through blockers; truly stuck → ask.
- Authenticated research = `webcap --user-data-dir=/run/host/home/eturkes/.config/browser-os`; access scope = signed-in browser, incl. university journals; keep that browser running; `chromiumfish` without the profile flag = isolated visual QA; surviving paywall/auth/human gate → ask + continue.
- Post-work cleanup: task-touched paths, esp. `$HOME`; remove temporary/stale artifacts + dangling symlinks.
- Headless capture = `webcap <url> [--pdf F] [--png F] [--dom F|-]` (`container/aeon/webcap`, CDP over chromiumfish): `--full-page` PNG; `--dark`; `--width`/`--height`; `--selector`/`--wait` settle; `--timeout`; `--user-data-dir`; fragment URLs scroll to target. Full real-site page → PDF + full-page PNG + DOM in ~3s, ~7s through `--user-data-dir`; Read the PDF. `--user-data-dir D` captures against a `cp --reflink` clone beside D → the host BrowserOS profile renders its signed-in session while the source stays byte-identical; 5GB clones in ~1.5s for no disk; keep that browser running. Profile access must go through `webcap`: it appends `--password-store=gnome-libsecret`, which Chrome's last-duplicate rule makes beat the `--password-store=basic` playwright-core pins into every launch on every platform; under `basic` a keyring-encrypted jar empties on open. That pin = explicit switch, not a detector → naming the store is the whole fix, and no desktop-environment variable substitutes. Going through `webcap` also preserves BrowserOS component extensions, which a foreign build garbage-collects. Chrome's own capture switches (`--print-to-pdf`, `--dump-dom`, `--screenshot`) hang in this build — page loads, browser stays live, rc=124, no artifact — under every GL/quiet/virtual-time flag set. GL is sound (`--use-angle=swiftshader` rasterizes text + backgrounds); SwANGLE/Vulkan/GCM stderr noise = benign. `--dark` promotes same-origin `prefers-color-scheme: dark` blocks to `all`; this build reports that query light under CDP emulation + `--force-dark-mode`; cross-origin stylesheets stay light + are reported; `matchMedia` stays light → matchMedia-themed pages need their own switch.
- `grep` = CC shell-fn shadow → `rg-fff` (**RE2**, relevance-ranked, fuzzy-fallback; see `# fff`), distinct from raw `ugrep -G`; `find` = CC shell-fn shadow executing `rg-fff` under `ARGV0=bfs` (GNU-compatible incl. `-printf`/`-regex`/`-exec`; errors prefix `bfs:`; GNU binary → `command find`) → always type `find`. PATH excludes `bfs`; literal `bfs …` = rc 127 + empty stdout, byte-indistinguishable from zero matches. `rg` = direct `/usr/bin/rg`; system `grep` = GNU grep (BRE). Byte-exact/clean → `command grep` | `/usr/bin/rg`.
- Byte-equality → prove with `cmp`/`sha256sum`; real diffs → `git diff --no-index`.
- Shell rc: capture + label immediately (`cmd; rc=$?`) before `printf`, substitution, or another command; every command overwrites `$?`. EMPTY-output findings (zero matches/processes/modifications/recent writes) → report rc + run a positive control. Missing command (127), mistyped path + unmatched glob emit the same bytes as a true negative.
- `pgrep -f`/`pkill -f` can self-match their `bash -c 'eval …'` wrapper → use one bracketed pattern (`index[.]js`) + `|| echo none` per command; separate kill/relaunch calls. CC's `pkill` guard blocks CLI-matching patterns.
- `bgcmd` (`~/.local/bin/`) = filesystem REPL, objects persist across separate Bash calls: `export BGCMDDIR=<dir> BGCMDPROMPT='>>> '` (re-export each call) → `bgcmd START <interp> -i -q` → `bgcmd '<oneliner>'` → `bgcmd 'exit()'; rm -rf "$BGCMDDIR"`.
- Scratch roster/log appends → `tee -a f`, `cat >> f`, `printf … >> f`, fileless construction (inline data as program args) or the Write tool; heredoc bodies (`<<'EOF'`) pass through every form.
- Deferred tools: session-start list = ground truth. Harness built-ins → best-guess call first; `InputValidationError` → fetch schema. **Every `mcp__…` name must be copied verbatim from the session list or from a `ToolSearch` `select:<name>` result** — a reconstructed one is the single tool-call error that kills the session (next bullet). `tool_search_tool_regex` (`pattern` = Python `re.search`) scopes to loaded schemas; its `No tools match` on a deferred name → load, then search. Bad tool use + error turn stay outside transcript/context; adjacent turns work → retry. `400 … unexpected tool_use_id` = history corruption → `/rewind`. System-prompt fixes load next session/compaction.
- Upstream tool-name cloaking = the one unrecoverable tool-call error; **`disable-claude-cloak-mode: true` in `cliproxyapi/config.yaml` disarms it, and is set**. The flag hot-reloads on write, is overridden by a credential file's own `cloak_mode`, and uncloaked requests declaring non-standard tool names are accepted `200`. It re-arms whenever the flag returns to `false` → **always spell a genuine MCP tool `mcp__<real-server>__<tool>`, and a built-in by its full listed harness name.** While armed, CLIProxyAPI aliases every tool name outside the `mcp__<a>__<b>` shape into `mcp__<W1>_<W2>__<W3>_<OrigName>`: `W1_W2` = BIP-39 virtual server keyed on the DOWNSTREAM caller's `api-keys` value, so it differs per proxy instance; `W3` = per-tool BIP-39 word; genuine `mcp__<server>__<tool>` names pass through as-is, and fuzzy recovery covers aliased entries alone. Response restore: exact map hit → restored; unrecognized server → forwarded unchanged, ordinary unknown-tool; **virtual-server prefix + tail matching no aliased built-in → `500 … cannot restore Claude OAuth MCP tool alias "<name>": no unique request-local match`, whole assistant turn discarded**. That shape = an MCP tool's bare basename wearing the built-ins' virtual-server prefix; a mixed namespace invites it, a deferred schema-unloaded MCP tool most of all. Deterministic → retries + `/resume` replay identical context → identical name → **session wedged; recovery = new session**, interim = drive the tool's own binary directly. Gate = `fp.MCPAlias && cloaked` in `internal/runtime/executor/claude_executor_execute.go`; failing request + its upstream tool list → `~/.cli-proxy-api/logs/error-v1-messages-*.log`.
- Finite background wait = `run_in_background: true` + `TaskOutput(task_id, block=true, timeout=…)`; completion also emits a `<task-notification>` with output-file path → `Read`. Foreground `until` loops auto-background → express polling as hook-accepted `until` loops. `timeout` = ms, max 600000; repeat 10-minute blocks; seconds-valued timeout returns immediately while task continues. Background command piped through `tail`/`head` buffers all output until exit → interim file empty; redirect to a log (`> file 2>&1`) + read it, or print unpiped.
- Waiting on long-running work → issue any tool call every ≤55 min: prompt-cache TTL = 1h, each call renews it; a lapse re-reads full context uncached.
- Serena user-scope MCP = live-LSP primary (nav/edit/rename/diagnostics; auto-install); project memory remains canonical. Missing language → `global` LSP marketplace-gap plugin; activate per project. At first touch, add real code + edited formats (markdown/toml/yaml/json) to `.serena/project.yml` `language_servers:`, then restart once (startup-only); retain formats with useful diagnostics. Headroom `wrap` owns the `serena` registration (user scope, PyPI `serena-agent`): re-created when absent, spec-migrated at wrap, removed by unwrap — leave it to wrap.
- Serena ops: missing target in `Active language servers` → add to `language_servers:` (first = fallback), restart, verify by symbol call. `replace_symbol_body` spans leading doc + outer `#[...]` attrs → include them or use `replace_content`. Missing/new `project.yml` keys trigger a full annotated rewrite; values persist → verify `language_servers:`/`ignored_paths:` + track the template. Serena owns `.serena/.gitignore` → commit `.serena/`; root `.gitignore` = project rules. Diagnostics: Serena-served formats require explicit `mcp__serena__get_diagnostics_for_file`; `global` plugin formats push passively. MCP registration passes `--project-from-cwd` → non-project cwd (e.g. `$HOME`) = `known projects: []` on every call → always `activate_project <abs path>` before symbol work. `restart_language_server`/`remove_project` = context-excluded → LS restart = reconnect the MCP server; deregister = edit `~/.serena/serena_config.yml` `projects:`.
- Serena MCP = per-session stdio spawn (`uvx` resolves PyPI `serena-agent` each start); a failed spawn = disconnected server + `~/.serena/logs/` untouched → health check = `claude mcp list` (fresh spawn reports Connected); recovery = `/mcp` reconnect, or the next session. Interim = `Read`/`/usr/bin/rg`; symbol + diagnostic work resumes at reconnect. Out-of-band `uvx --from serena-agent serena <cmd>` = index/config/memory maintenance.
- Serena file/bulk edits: extensionless script + empty symbol overview → use `Read`/`rg`. `replace_in_files.paths_include_glob` remains project-root-relative when `relative_path` narrows selection → prefix the directory or select unrestricted paths. Dry-run `0 file(s)` = selection mismatch; `0 occurrence(s)` = text mismatch.
- Marksman = Serena's bundled Markdown link LSP; fixed config, pull diagnostics source `Marksman`; all findings = code 2/Warning → keep the full signal. Fixes: bare `][` outside code parses a phantom reflink → backtick notation; missing document → repair link; >1 H1 target → retain one H1. Bare `<nonterminal>` → backtick-wrap (parser treats it as HTML; link lint skips it). Byte-authoritative edge-space code spans → visible ␣=U+0020 legend or fence (CommonMark trims one pair). Index honors `.ignore`/`.gitignore`/`.hgignore` at startup; keep Markdown targets index-visible and use `.rgignore` for rg-only hiding. Disable: remove `markdown` from `language_servers:` + restart.
- Markdown gate: prefer `markdown` active in Serena + pull `mcp__serena__get_diagnostics_for_file`. Before activation, check bare `][` outside code, Markdown links resolving to existing `.md` (write other paths inline), and one H1/file. Authoritative stopgap (binary lands with Serena's first `markdown` LS start): find it (`find ~/.serena -name marksman -type f`); run `marksman server`; send `initialize` → `initialized` → `textDocument/didOpen`; read `publishDiagnostics`. A `.git`/`.marksman.toml` root enables cross-file links. Seed one broken link as a positive control because a clean file is silent; silence = no `publishDiagnostics` at all, so a blocking read hangs → reader thread + idle timeout. Serena runs its own `marksman` off that path → check PPID before killing a stray. Marksman emits all findings together + indexes every committed `.md`, including `.agent/`.
- Headroom = patched local build (image-worker hard-termination fix): the uv receipt path-pins the `~/src/headroom` dist wheel → `uv tool upgrade` holds the pin + needs that wheel on disk; keep it until an upstream release ships the fix (PyPI reinstall sheds it). Image-compression off-switch = `--no-image-optimize`/`HEADROOM_NO_IMAGE_OPTIMIZE`; state + knobs + recipes → `~/agents/container/aeon/headroom/README.md`.
- Docs mirror `~/agents/docs/<site>/llms.txt` (scopedcommits.com, agentlanguages.dev) > web fetch.

## Reading

- Reader gates: `permissions.deny` = pagers (`less`/`more`/`most`/`pg`), colorizers (`bat`/`batcat`/`ccat`/`pygmentize`/`highlight`/`source-highlight`) + TTY editors (`vi`/`vim`/`nvim`/`view`/`nano`/`emacs`/`ed`), matched in every Bash segment incl. pipes + compounds → reach those bytes with `Read`. Keep it Bash-only: a path-keyed `Read()` rule gates `Bash` by static command text, so a batched command dies for naming the path it excludes, and one such rule escalates every command whose cwd it cannot resolve (`cd <unresolvable> && <cmd> <relpath>`) into a session-halting prompt under `bypassPermissions` — existing is the trigger, matching is not required. `cat`/`head`/`tail` shell-fn guards (`claude/read-guard.sh` ← `~/.profile` → CC snapshot) budget output volume rather than block names: stdin pipelines, operands totalling ≤4096 B, `-n ≤100` + `-c ≤4096` all run native; oversized operands, `tail -f` + unmeasurable paths (device/fifo/dir) route to `Read`. `command cat` always bypasses, and redirection stays invisible to a shell function → write copies with `command cat big > copy`. `quiet-bash` rewrites pipe-source `cat f | consumer` to `< f consumer` first, so a bounding consumer keeps a file of any size. Bounded transforms + byte inspectors run free: `nl`/`tac`/`rev`/`fold`/`pr`/`fmt`/`od`/`xxd`/`hexdump`/`hd`/`hexyl`/`strings`. Headroom keeps `Read` lossless + reclaims stale/superseded bytes (file-path keyed); Bash output = compression target + remains resident.
- `@path` (typed input + slash-command bodies) attaches the whole file <262144 B as a mention-time snapshot; `@path#L10-20` = that line range. Oversize + missing path = one silent no-attachment outcome → prove absence with `ls` before recording state absent. Dense markdown ≈3 B/token.
- Command economy: every run output rides the session → always use the quietest useful form: quiet/dot reporters (`pytest -q`, `cargo -q`, `make -s`, `bun --silent`, `curl -sS`; npm + pnpm defaults preserve failure text at ~75/190 non-TTY bytes; silent modes suppress it); `--stat`/`--name-only` over full diffs; `-c`/`-l`/`--include` over bodies; `| head -N` on unbounded listings; tool-side filters over dumps. Bulk output → redirect + read a slice, or delegate. Pipes move rc to last stage → add `set -o pipefail` or read `${PIPESTATUS[0]}` when runner status matters. `~/.claude/quiet-bash` injects quiet flags + rewrites pipe-source `cat f |` to a leading `< f `; user supplies pipes. A heredoc stands the whole hook down; verbose/debug flags hold flag injection alone → explicit verbosity survives.
- Text inside a binary (Claude CLI JS bundled into `~/.local/share/claude/versions/<v>`) → `/usr/bin/rg -a -o '<pat>.{0,400}'`; `-a` yields matching lines, while plain `rg` yields only `binary file matches`. `Read` returns mojibake → prefer `rg -a`; `strings`/`xxd`/`od` run free for byte-level questions `rg` leaves open. Widen `.{N}` on both sides to walk minified call sites.
- Headroom reads = lossy/reversible. `[N … hash=…]` → `mcp__headroom__headroom_retrieve`; hashless `[N lines omitted: …]` → narrow `Read`/`grep -n`. Treat compressed output as browse context. Byte-exact `Edit.old_string` → retrieve/narrow Read/grep/sed/`read_text()`, anchored on short distinctive bytes; compression rewraps prose and can mislead with `\uXXXX` hints. Expired/evicted hash 404 → narrow reads/Python.
- Structured files (JSON/arrays — permission/deny lists, lockfiles, manifests) → always prove content + disk shape before editing: `wc -c`; `jq '.<path>|length'`; `jq -r 'to_entries[]|"\(.key+1)|\(.value)"'` (ordinal lines survive intact); line count via `command grep -c ''` / `git show HEAD:<path> | wc -l`. Entry count/byte mismatch = partial view. Bash JSON can collapse to one markerless line + omit interior entries; later `Read`/`jq .` repeats it → editing that view deletes omitted entries; Read→Write minifies + truncates. Trust length + ordinal lines; reach exact bytes through anchored Edit; `git diff --stat` churn beyond intended edit = tell.
- Retrieve tool's sole name = full `mcp__headroom__headroom_retrieve` (server + tool segments both read `headroom`; naming rule → `# Environment`). Large retrieve may return `<<ccr:…,NN KB>>` → use narrow `Read`/`grep -n`/Python.
- Quote YAML frontmatter scalars opening with an indicator char (`[ { } ] , & * ! | > % @ # :`, backtick, double-quote): leading `[` → flow sequence → `ParserError` or silently-dropped field. Verify ad-hoc frontmatter with an ephemeral `pyyaml` parse.
- `API Error: <ConnectionTerminated error_code:0 …>` = transient HTTP/2 GOAWAY mid-stream through Headroom; context survives → `git status`, resume.
- Context occupancy = latest assistant `usage` sum (input + cache create/read + output), often above transcript size because system/tools/CLAUDE overhead + redacted reasoning stay billed. Server-tool iterations (e.g. ToolSearch) stack cache-read by ~1 window/iteration → measure from an ordinary turn. `context-gauge` = sum; `jq .message.usage "$(context-gauge -p)"` = parts; effort shifts usage/stored ratio → remeasure. Auto-compaction fires when last usage + pending payload crosses 240K (warning 220K); large payload or iteration inflation can trigger early → target 200K + 40K reserve; discover tools early.

## fff search (`rg-fff` — shadows `grep`)

CC maps `grep`→`rg-fff`: default fff RE2, relevance-ranked + marked. Serving requires repo cwd; other contexts defer to ugrep. `rg`=`/usr/bin/rg`. Per-repo daemon auto-spawns. Fresh writes can lag its index → use `command grep` or re-query.

- **Output ≠ real grep** → RANKED by relevance over file-order, strips leading `./`, inline markers, lines capped 512B. Clean/order-stable/byte-exact/full-line → `command grep` (=GNU grep BRE, `./`, file-order, untruncated) | `/usr/bin/rg`.
- **Zero-match grep ≠ empty (BIG)**: auto fuzzy-fallback → `# rg-fff: 0 EXACT…` hdr + N ` [~approx]` lines on **STDOUT** (survive `2>/dev/null`, flow into pipes/`$()`), shaped like real `path:line:text`. Truth = **exit 1** + `[~approx]`/leading-`#` (lines appear regardless) → piping/capturing a maybe-zero grep MUST filter `[~approx]`/`#` or use `command grep`. Off: `RG_FFF_NO_FUZZY_FALLBACK=1`.
- **`[def]`** trailing tag = likely definition; rank treats it like any other line — junk/long lines can outrank it → trust the tag over position; "read top hit first" misleads here.
- **>512B line** → served truncated + ` [...rg-fff: line truncated at ~512B; Read the file for the full line]` (`-c` counts once); non-UTF-8 (U+FFFD) long line defers → Read full file.
- **Served**: literals incl. phrases w/ space/`:`/quote (regex-escaped), RE2 regex (`\b \d \w + * ( ) | {} ?`, empty-capable `x?` OK), `-i`, `-A/-B/-C N`, `-l`, `-c`, `--include=*.ext`, reldirs + `.`. **Serve requirement: add `-r`/`-R` to every fff query.** Verify via `RG_FFF_LOG=<path>` (`served-daemon`/`served-cold`); a missing flag yields `fallback-ineligible` + deferred BRE, where RE2 syntax can silently misfire (`grep -c 'a|b' <dir>` = 0, exit 1).
- **DEFERS → embedded ugrep -G = BRE** (clean, file-order, marker-free): universal defers = missing `-r`/`-R` or non-repo cwd; others = stdin/pipe (`…|grep` ALWAYS), single-file arg, `-o`, `-P`/PCRE, newline-matching regex (`\s`/negated class), non-ASCII pat, `--no-ignore`/`--include-dir`, absolute path, glob ≠ `*.ext`. **Deferred regex = BRE** → RE2/ERE syntax misfires (piped `foo|bar` matches literal `foo|bar`).
- **Served dialect = RE2** → bare `|`=alt, `+ * ?`=quantifiers, `()`=group, `\d \w `=classes; `\|` `\+`=literals. `command grep` / deferred BRE: `\|`=alt, `\+`=1+.
- `/usr/bin/rg` direct: recurses by default → pass `<pat> <path>` alone. Its `-r` = `--replace`; carrying fff's required `-r` consumes the pattern as replacement + promotes path to pattern → readable stdin blocks; `.` rewrites every line to replacement (rc 0, fabricated match-shaped bytes); named dir = rc 1 + empty stdout. Name dot-dirs (`.agent/`, `.scratch/`) explicitly; explicit paths search regardless of hidden/ignore state; tree sweep → `--hidden`; gitignored dot-dirs require `-uu` (`--hidden --no-ignore`).
- Knobs: `RG_FFF_FIRST=0` = regex→byte-equiv BRE-mirror (literals STAY fff-ranked+marked); `RG_FFF_LOG=<path>` per-call decision log (`served-daemon`/`fallback-*`); `RG_FFF_DEBUG=1` serve/defer reason on stderr.

## Subagents

- Delegation scope = large tracks; one subagent when sufficient; low spawn count; chunk around rate limits. Harvest = read report + same-call `TaskStop`: DONE ends report; `TaskStop` ends agent/write access. Close order = stop all + retire watchers → prove quiescence → read `git -C <wt> status --porcelain` per worktree in its OWN call, the last point the content still exists → remove worktrees + `wt/` branches → decisive rerun → commit; this keeps recorded state valid against late writes. `TaskStop` cuts mid-tool-call ⇒ a teammate's final `git commit` can be issued yet never execute (transcript ends on `tool_use` w/o `tool_result`; tip sits one commit back) ⇒ treat any non-empty status as content + commit it on its `wt/` branch. Recovery from a destroyed worktree = replay the transcript's `Edit` calls onto the last real commit + validate against an oracle the replay never read. Owning scope = this bullet; mode/project-loop docs cite it + add bindings only (harvest content, ledgers, gate command).
- Script-derivable facts (counts, spans, censuses, call-site inventories, hashes) → lead computes directly: a delegated number still needs the lead's own verification, so delegation pays brief + poll + harvest + verify for what one script returns. Delegate judgment, bulk reading + parallel tracks.
- Name every lead dispatch → dead teammate revives in place via `SendMessage` with retained context + same-channel steering; anonymous dispatch forfeits that map. Agents execute assigned scope; lead approval gates re-delegation; teammate-internal subagents = anonymous (completion → inline `<result>`). Cross-session communication relays through user/current lead.
- Teammate protocol: `Agent` returns spawn metadata; completion/death = pull-only. Dispatch assigns `<NAME>-DONE-<i>` (agent name verbatim; fresh each dispatch) + report destination (scratch for findings/evidence; working tree for code). Brief + `SendMessage` text lands verbatim → name files by plain repo-relative path. Counters: role `-<k>` = gapless instance ordinal; marker `<i>` = per-instance iteration → name↔instance map survives compaction. Poll transcript's last assistant text (`jq -r 'select(.message.role=="assistant") | .message.content[]? | select(.type=="text") | .text' <t>.jsonl | tail`); raw `rg` hits spawn/`SendMessage` copies; anchored `"text":"<MARKER>` misses terminal markers. Watcher = one transcript + marker; match `MARKER|API Error`; death match → apply death cadence immediately; the `TaskStop`/supersession call also retires watcher, or survivor can impersonate successor completion. Validate signal against awaited transcript. Prove watcher death via `pgrep -af '<MARKER-fragment>'` + kill surviving PID; `tail -N` can omit an old marker + loop indefinitely, so roster `fired+exited` = note ≠ evidence. Completion = marker + ~90s transcript silence; resumed agent can retain stale DONE while tools write. `SendMessage` = plain text + required `summary`; any message to completed agent resumes tools/write access invisibly to lead → `TaskStop` at harvest. `TaskOutput` = background Bash IDs. Reports: `ls` verifies file; absent file → re-request exact path once, then persist transcript text. Evidence artifacts lack source-state binding → rerun/re-derive; otherwise browse context.
- Delegation boundary block: each item gets its own directive line — git posture (`leave every change unstaged in the working tree; the lead alone runs git add/commit`), write allowlist, gate + spec surfaces marked `read-and-conform-to` (agent reports suspected error → lead rules). Unspecified posture defaults to agent staging/committing + editing neighbouring coordination files; acceptance phrased as `gates green` ⇒ it edits the gate. Parentheticals = advisory. Lead brief honors the same split; delegating a lead-retained file reframes retained boundaries as negotiable. Breach 1 → revert + restate; breach 2 by same agent → `TaskStop` + fresh successor (saturated agents rationalize rather than re-read).
- Shared mutable resources (build/target stores, DBs, ports, work tree, live browser profile) = one named + enumerated holder/wave in every brief; `analysis-only` means tracked bytes stable while shared-store rebuild remains permitted. Unassigned tokens waste turns on `ps`/`pgrep`. Reported underfoot changes = CONTENTION until disproven → trust reporter; sweep every spawned transcript mtime on every poll (`find "$HOME/.claude/projects/$(pwd -P | tr '/.' '--')"/*/subagents -name '*.jsonl' -mmin -2`). `-mmin` works under shadow + GNU; relative `-newermt '-90 seconds'` = GNU-only. Artifact stillness proves its sampled window alone. Quiet proof = empty recent sweep + wide-window rerun (`-mmin -100000`) listing roster transcripts; else glob mismatch.
- Worktree isolation — use where a wave writes code: one `git worktree` per teammate, lead alone writing the primary tree, which confines the contention rules above to it. `git worktree add -b wt/<name> <scratch>/worktrees/<name>` = tracked-content sized + sub-second, since build outputs + environments stay untracked; the brief carries that path as the teammate's whole world. Give each worktree private cache/coverage/state paths + point its toolchain env at the primary environment read-only → every worktree runs the full gate concurrently off one shared environment. Teammates commit in-worktree per checkpoint; baseline restore = `git reset --hard` + `git clean -fd` back to the last checkpoint, new files included. Harvest merges content with `git merge --squash wt/<name>`; a successor inherits its predecessor's worktree, whose fetched pages + derived data outlive the death that stranded them.
- Permission mode = `bypassPermissions` everywhere (settings `defaultMode` + agent dispatch); `permissions.deny` still fires.
- Fixed subagent model + effort: every subagent / teammate runs `gpt-5.6-sol` — settings env pin = authoritative, per-call `model` inert; `model` = blank so effort env carries through.
- Scope each agent ≤~200K with auto-compaction safety; work stays steerable post-compact. Gauge every live role each poll. >~180K → checkpoint report + hand off to fresh same-role successor. Report emitted >~90% = context-starved (compressed, follow-up-incapable) → re-derive. Split read+rewrite at section boundaries (~100K peak on ~40KB/~37%); ~180 web calls ≈ one window → cap research near ~100. Transcripts = `~/.claude/projects/<project>/<session>/subagents/agent-a<name>-<hash>.jsonl`. Health = `.message.usage`; reasoning blobs ≈100× content. `context-gauge [-p] [<name>]` (`~/.local/bin`, cwd-scoped): bare = `N% NK/240K` (MAIN last turn, teammate high-water); `-p` = transcript; unresolved = `? ?/240K`; mid-run assistant text ≈ nil → harvest reports/diffs.
- Deliverable-first checkpointing, every deliverable incl. code: brief declares unit total + batch size + ONE artifact the lead commits before dispatch so the agent's first tool call fills it — table/report = full final-format skeleton (every target row, each cell `unknown`); code = a committed stub naming every intended unit with a failing body. Fill cells + commit units in place every batch, ≤ every N tool calls. Ship unit 1 by simplest means; fund tooling after first flush. Generate the skeleton from the validator's own schema, grade it both ways at seed (all-`unknown` → nonzero, filled → 0), and name that command in the brief. The seed is the whole variable: delivering configuration = seed + validator as one pair; measured yield of a prose mandate, a validator alone, or `gather all → derive → emit` = 0 units. Lead polls unfilled cells (`grep -c unknown`) + committed units = the only counters that move under in-place filling, which holds report line count flat. Flat across 2 polls or lagging gauge → one flush directive, then succeed at the next poll whether or not it complied, since repeated steering saturates rather than lands; fund the successor only for scope no surviving peer already covers. `SendMessage` lands at turn boundary, so brief encodes corrections + report stays control surface. Transcript-only unflushed work → rerun; disk artifacts persist for successor.
- gpt-5.6-sol death shape: agent's final assistant text = `API Error: … help.openai.com … request ID …` | `API Error: 503 auth_unavailable … (providers=codex, model=gpt-5.6-sol) … gateway (127.0.0.1:8787)` = provider-side flapping. Distinct class: `400 … flagged for possible cybersecurity risk … chatgpt.com/cyber` = content classifier on the REQUEST (security-flavored vocabulary/material in context — escape-scan/bypass/exploit wording, token-scanner sources); permanent for that context, revive re-dies; successors re-die once they ingest the same material even under a reworded brief → route security-vocabulary work (trust-gate reviews, hardening audits) to MAIN/Claude-side execution or sanitize every input artifact first (`~/.codex/auth.json` JWT stays valid; 8787 gateway = MAIN's own transport — leave running). Respond with cadence: revive (`SendMessage`) on sight; instant re-death = active outage → retry every few minutes while MAIN keeps working; 2 consecutive re-deaths or transcript >~180K → switch to fresh successor from teammate's checkpoint/report (fresh + small outlives revive under flapping). Deliverable-first checkpointing ⇒ death anytime = respawn-cheap.
- WebSearch budget = 200/session (`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`), shared by MAIN + every subagent; two research agents at ~75–80 calls each exhaust it — over-budget WebSearch fails while other web tools keep working → ration an explicit per-agent allowance on research fan-outs.
- Main-session auto-compaction: background agents + shells continue; ids + `TaskOutput`/notifications stay valid. Harness re-announces agent ids + partial-output paths after boundary; record background-shell ids yourself. Multi-agent fan-out → scratch roster; near limit, externalize shell ids + expected results too.

## Output

- Chat: focused + brief. Keep disclaimers/caveats short, spend the response on the main answer; "explain X" → high-level summary, going in-depth on request.
- Progress updates: one sentence before first tool call. Mid-work trigger = real find or direction change. Closing starts with outcome; first sentence answers what happened/found, then evidence.
- Written deliverables (reports, md docs, summaries) → cover substance at task-fit length.

## Meta

- Symlinks: `Read` follows; `Edit`/`Write` on a symlinked file fail (`Refusing to write through symlink`) → edit `readlink -f` target, use a symlinked parent, or write through Bash.
- `Edit` line deletion → trailing-newline form (`victim\n`→``) or anchor-keeping replace (`anchor\nvictim`→`anchor`); leading-`\n` old_string + empty new_string also eats the trailing newline → neighbors merge on a success-reported edit (multi-line inserts clean). Post-edit `command grep -n` a neighbor line.
- My direct instructions outrank any `CLAUDE.md`.
