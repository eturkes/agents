# global

User-global Claude Code LSP marketplace. Plugins here are enabled at the user
scope (`~/.claude/settings.json`) and are available across all projects.

Registered as a `directory` marketplace pointing at this folder. Server binaries
must be present on PATH; see each plugin README for install commands.
