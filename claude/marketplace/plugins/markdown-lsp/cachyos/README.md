# Markdown language server on CachyOS

Marksman reports broken links, duplicate headings, and unresolved references in Markdown, Quarto, and R Markdown files.

## Prerequisites

1. Run `sudo pacman -S --needed marksman`.
2. Confirm that `marksman --version` prints a version.

`paru -Syu` upgrades the package with the system.
